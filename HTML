<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ALSRS</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>

    <div class="container">
        <h1>ALSRS</h1>
        <button id="loginButton">LOGIN</button>
        <button id="citizenLoginButton">CITIZEN LOGIN</button>
        <button id="newUserButton">NEW USER</button>

        <!-- Login Form -->
        <div id="loginForm" class="hidden">
            <h2>Login</h2>
            <input type="text" id="username" placeholder="Username" required>
            <input type="password" id="password" placeholder="Password" required>
            <button onclick="authenticateUser()">Submit</button>
            <p class="error" id="loginError"></p>
        </div>

        <!-- New User Registration Form -->
        <div id="newUserForm" class="hidden">
            <h2>New User Registration</h2>
            <input type="email" id="newEmail" placeholder="Email (e.g., example@domain.gov)" required>
            <input type="text" id="newUsername" placeholder="Username" required>
            <input type="password" id="newPassword" placeholder="Password" required>
            <input type="password" id="retypePassword" placeholder="Retype Password" required>
            <button onclick="registerUser()">Register</button>
            <p class="error" id="registerError"></p>
            <p class="success" id="registerSuccess"></p>
        </div>

        <!-- Login Page with Patient Records Table -->
        <div id="loginPage" class="hidden">
            <h2>Patient Records</h2>
            <table>
                <thead>
                    <tr>
                        <th>S.no</th>
                        <th>Date</th>
                        <th>Time</th>
                        <th>Patient Name</th>
                        <th>Age</th>
                        <th>Sex</th>
                        <th>Status</th>
                    </tr>
                </thead>
                <tbody id="patientRecords">
                    <!-- Patient records will be loaded here -->
                </tbody>
            </table>
            <button id="addRecordButton">Add Record</button>
        </div>

        <!-- Citizen Information Page -->
        <div id="citizenPage" class="hidden">
            <h2>Citizen Information</h2>
            <p>Working of ALSRS:</p>
            <p>Includes a 3D model and demonstration of CPR functionality.</p>
        </div>
    </div>

    <script src="script.js"></script>
</body>
</html>
